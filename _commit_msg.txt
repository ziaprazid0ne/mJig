Signal reuse de-duplication, dialog helper extraction, bug fixes

- Added Register-UserInput, Test-UserInputActivity, Add-LogEntry, Send-ViewerMessage, Invoke-GlobalHotkeyAction helpers to eliminate duplicated input/log/IPC boilerplate
- Added Invoke-DialogResize wrapper; converted all 8 dialogs to use it
- Added Write-DialogButton and Write-DialogFrame renderers; replaced hand-drawn button and border code across all 8 dialogs
- Cached Write-MainFrame chrome/menu/log-stats geometry in $script:_FrameLayout; invalidate on resize/theme/output/title/sleep/debug change
- Unified $script:LastUserActivityTime as the single idle clock; removed parallel timer variables
- Extracted Invoke-DialogHotkeyAction for shared pause/sleep/quit handling between worker and inline paths
- Fixed Register-UserInput crash: removed [ref] type annotation from optional ref parameters (PowerShell coercion gotcha)
- Fixed viewer crash on dialog open: removed [string] annotation from nullable color params in Write-DialogButton; hardened Write-Buffer against empty-string colors
- Fixed Options dialog click detection: $clickY - $dialogY row calculation was missing, causing all toggle-row clicks to be silently ignored
- Added signal-reuse.mdc rule file; updated AGENTS.md, README, CHANGELOG, and all .cursor/rules files

See CHANGELOG.md for full details.
